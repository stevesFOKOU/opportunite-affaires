# Configuration de la base de données
# En local (SQLite) - pour le développement, changez le provider dans prisma/schema.prisma à "sqlite"
# DATABASE_URL="file:./dev.db"

# En production (PostgreSQL) - fourni automatiquement par Render
DATABASE_URL="postgresql://user:password@host:5432/database"

# Secret JWT pour les tokens d'authentification
JWT_SECRET="votre-secret-jwt-tres-long-et-securise"

# Environnement
NODE_ENV="development"

# Port du serveur (optionnel, défaut: 3001)
PORT=3001

# Compte admin par défaut
ADMIN_EMAIL="admin@opportunite-affaires.com"
ADMIN_PASSWORD="admin123"
